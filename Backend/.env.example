# Server
PORT=5000
MONGO_URI=
JWT_SECRET=
FRONTEND_URL=http://localhost:5173

# SMTP (Nodemailer)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
# Gmail app password: use 16 chars with NO spaces, or quote if pasted with spaces:
# SMTP_PASS="abcd efgh ijkl mnop"
SMTP_PASS=
SMTP_FROM=
